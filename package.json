{
  "name": "gtm-data-engine",
  "version": "0.1.0",
  "private": true,
  "description": "GTM Data Enrichment Engine — ingest, resolve, enrich, verify, activate.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/api/server.ts",
    "build": "tsc -p tsconfig.json && npm --prefix web install && npm --prefix web run build",
    "start": "node dist/src/api/server.js",
    "start:prod": "npm run db:migrate && npm start",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "lint": "eslint . --ext .ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "engine": "tsx src/engine/cli.ts",
    "seed:eso": "tsx scripts/seed-eso.ts",
    "seed:sequences": "tsx scripts/seed-sequences.ts",
    "classify": "tsx scripts/classify.ts"
  },
  "dependencies": {
    "csv-parse": "^6.2.1",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.36.0",
    "express": "^4.21.0",
    "jose": "^6.2.3",
    "postgres": "^3.4.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
